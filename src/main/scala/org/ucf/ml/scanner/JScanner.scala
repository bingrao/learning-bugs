package org.ucf.ml
package scanner

import com.github.javaparser.JavaToken

class JScanner(tokens:List[JavaToken]) {
  def tokenize() = {
    val sb = new StringBuilder()

    sb.toString().trim
  }
}